{"Shader":{"ver":"0.1","info":{"id":"llcGDl","date":"1472312000","viewed":104,"name":"One pass blur","username":"CaptCM74","description":"Experimenting with iterators and samplers.\nThe result is pretty ugly.\n\nNo credits required because, Why not? it's simple as just iterating.","likes":0,"published":3,"flags":0,"tags":["blur","onepass"],"hasliked":0},"renderpass":[{"inputs":[{"id":5,"src":"\/presets\/tex04.jpg","ctype":"texture","channel":0,"sampler":{"filter":"mipmap","wrap":"repeat","vflip":"true","srgb":"false","internal":"byte"}}],"outputs":[{"id":37,"channel":0}],"code":"void mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n\tvec2 uv = fragCoord.xy \/ iResolution.xy;\n    \n    float hor = 1. \/ iResolution.x;\n    float ver = 1. \/ iResolution.y;\n    const float iter = 5.;\n    \n    \n    vec4 pic = texture2D(iChannel0,uv)\/(iter * 10.);\n    \n    for (float i = 0.;i < iter;i++)\n    {\n    pic += texture2D(iChannel0,uv+vec2(hor*i,0.0)) \/ (iter * 10.);\n    pic += texture2D(iChannel0,uv+vec2(-hor*i,0.0)) \/ (iter * 10.);\n    pic += texture2D(iChannel0,uv+vec2(0.0,ver*i)) \/ (iter * 10.);\n    pic += texture2D(iChannel0,uv+vec2(0.0,-ver*i)) \/ (iter * 10.);\n    \n    pic += texture2D(iChannel0,uv+vec2(hor*i,ver*i)) \/ (iter * 10.);\n    pic += texture2D(iChannel0,uv+vec2(-hor*i,ver*i)) \/ (iter * 10.);\n    pic += texture2D(iChannel0,uv+vec2(hor*i,-ver*i)) \/ (iter * 10.);\n    pic += texture2D(iChannel0,uv+vec2(-hor*i,-ver*i)) \/ (iter * 10.); \n    }\n    \n\tfragColor = vec4(pic);\n}","name":"Image","description":"","type":"image"}]}}