{"Shader":{"ver":"0.1","info":{"id":"XsKGzK","date":"1455371820","viewed":275,"name":"Ripple shader","username":"kzy","description":"Press W + click and drag to draw walls.\nPress Q to reset.","likes":2,"published":3,"flags":48,"tags":["water","ripple"],"hasliked":0},"renderpass":[{"inputs":[{"id":6,"src":"\/presets\/tex05.jpg","ctype":"texture","channel":3,"sampler":{"filter":"mipmap","wrap":"repeat","vflip":"true","srgb":"false","internal":"byte"}},{"id":8,"src":"\/presets\/tex07.jpg","ctype":"texture","channel":1,"sampler":{"filter":"mipmap","wrap":"repeat","vflip":"true","srgb":"false","internal":"byte"}},{"id":258,"src":"\/presets\/previz\/buffer01.png","ctype":"buffer","channel":0,"sampler":{"filter":"linear","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"}},{"id":259,"src":"\/presets\/previz\/buffer02.png","ctype":"buffer","channel":2,"sampler":{"filter":"linear","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"}}],"outputs":[{"id":37,"channel":0}],"code":"#define BUFFER_U iChannel0\n\n#define LIGHT vec3(3.0, 2.0, -2.0)\n#define DEPTH 0.5\n\n#define USE_BACKGROUND\n\nfloat unpack(const in vec4 value)\n{\n    return value.a;\n}\n\nfloat get(in sampler2D tex, in vec2 uv)\n{\n    return unpack(texture2D(tex, uv));\n}\n\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord)\n{\n\tvec2 uv = fragCoord.xy \/ iResolution.xy;\n    \n    vec2 p = 1.0 \/ iResolution.xy;\n    \n    float p0 = get(BUFFER_U, uv + p * vec2(-1, 0));\n    float p1 = get(BUFFER_U, uv + p * vec2( 1, 0));\n    float p2 = get(BUFFER_U, uv + p * vec2( 0,-1));\n    float p3 = get(BUFFER_U, uv + p * vec2( 0, 1));\n\t\n    vec3 n = normalize(vec3(2.0 * (p0 - p1), 1.0, 2.0 * (p2 - p3)));\n\n#ifdef USE_BACKGROUND\n    vec3 c = texture2D(iChannel1,uv + n.xz * DEPTH).xyz;\n#else\n    vec3 c = vec3(dot(n, normalize(LIGHT)));\n#endif\n    if (texture2D(iChannel2, uv).a > 0.0)\n    \tc = texture2D(iChannel3, uv).xyz;\n    \n    fragColor = vec4(c, 1);\n}","name":"Image","description":"","type":"image"},{"inputs":[{"id":33,"src":"\/presets\/tex00.jpg","ctype":"keyboard","channel":2,"sampler":{"filter":"linear","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"}},{"id":257,"src":"\/presets\/previz\/buffer00.png","ctype":"buffer","channel":0,"sampler":{"filter":"linear","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"}},{"id":258,"src":"\/presets\/previz\/buffer01.png","ctype":"buffer","channel":1,"sampler":{"filter":"linear","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"}},{"id":259,"src":"\/presets\/previz\/buffer02.png","ctype":"buffer","channel":3,"sampler":{"filter":"linear","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"}}],"outputs":[{"id":257,"channel":0}],"code":"\/\/ v buffer\n\n#define BUFFER_V iChannel0\n#define BUFFER_U iChannel1\n\n#define DAMPING 0.99\n\n#define RESETKEY 81.5\/256.0\n\n\/\/const float c = 1.0;\nconst float dt = 0.5;\n\nvec4 pack(const in float value)\n{\n    return vec4(0, 0, 0, value \/ 2.0 + 0.5);\n}\n\nfloat unpack(const in vec4 value)\n{\n    return value.a * 2.0 - 1.0;\n}\n\nfloat get(in sampler2D tex, in vec2 uv)\n{\n    return unpack(texture2D(tex, uv));\n}\n\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord)\n{\n    vec2 uv = fragCoord.xy \/ iResolution.xy;\n\n    vec2 p = 1.0 \/ iResolution.xy;\n\n    float c = 1.0 - texture2D(iChannel3, uv).a;\n    \n    float f = 0.0;\n    f += get(BUFFER_U, uv + p * vec2(-1, 0));\n    f += get(BUFFER_U, uv + p * vec2( 1, 0));\n    f += get(BUFFER_U, uv + p * vec2( 0,-1));\n    f += get(BUFFER_U, uv + p * vec2( 0, 1));\n    f -= get(BUFFER_U, uv) * 4.0;\n    f *= c * c;\n    \n    float v = get(BUFFER_V, uv);\n    v += f * dt;\n    v *= DAMPING;\n    \n    if (iFrame == 0 || iGlobalTime < 1.0 || texture2D(iChannel2, vec2(RESETKEY, 0.5)).x > 0.0)\n        v = 0.0;\n    \n    fragColor = pack(v);\n}","name":"Buf A","description":"","type":"buffer"},{"inputs":[{"id":33,"src":"\/presets\/tex00.jpg","ctype":"keyboard","channel":2,"sampler":{"filter":"linear","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"}},{"id":257,"src":"\/presets\/previz\/buffer00.png","ctype":"buffer","channel":0,"sampler":{"filter":"linear","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"}},{"id":258,"src":"\/presets\/previz\/buffer01.png","ctype":"buffer","channel":1,"sampler":{"filter":"linear","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"}}],"outputs":[{"id":258,"channel":0}],"code":"\/\/ u buffer\n\n#define BUFFER_V iChannel0\n#define BUFFER_U iChannel1\n\n#define RESETKEY 81.5\/256.0\n\nconst float dt = 0.5;\n\nvec4 pack(const in float value)\n{\n    return vec4(0, 0, 0, value \/ 2.0 + 0.5);\n}\n\nfloat unpack(const in vec4 value)\n{\n    return value.a * 2.0 - 1.0;\n}\n\nfloat get(in sampler2D tex, in vec2 uv)\n{\n    return unpack(texture2D(tex, uv));\n}\n\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord)\n{\n    vec2 uv = fragCoord.xy \/ iResolution.xy;\n    \n    float u = get(BUFFER_U, uv);\n    float v = get(BUFFER_V, uv);\n    \n    u += v*dt;\n    \n    if (iFrame == 0 || iGlobalTime < 1.0 || texture2D(iChannel2, vec2(RESETKEY, 0.5)).x > 0.0)\n        u = 0.0;\n    if (distance(fragCoord.xy, iMouse.xy) < 1.0 && iMouse.z > 0.0)\n        u = 1.0;\n    \n    fragColor = pack(u);\n}","name":"Buf B","description":"","type":"buffer"},{"inputs":[{"id":33,"src":"\/presets\/tex00.jpg","ctype":"keyboard","channel":1,"sampler":{"filter":"linear","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"}},{"id":259,"src":"\/presets\/previz\/buffer02.png","ctype":"buffer","channel":0,"sampler":{"filter":"linear","wrap":"clamp","vflip":"true","srgb":"false","internal":"byte"}}],"outputs":[{"id":259,"channel":0}],"code":"#define DRAWKEY 87.5\/256.0\n#define RESETKEY 81.5\/256.0\n\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord)\n{\n    vec2 uv = fragCoord.xy \/ iResolution.xy;\n    vec4 color = texture2D(iChannel0, uv);\n    \n    if (iFrame == 0 || iGlobalTime < 1.0 || texture2D(iChannel1, vec2(RESETKEY, 0.5)).x > 0.0)\n        color.a = 0.0;\n    if (texture2D(iChannel1, vec2(DRAWKEY, 0.5)).x > 0.0 && distance(fragCoord.xy, iMouse.xy) < 10.0 && iMouse.z > 0.0)\n        color.a = 1.0;\n    \n    fragColor = vec4(color);\n}","name":"Buf C","description":"","type":"buffer"}]}}